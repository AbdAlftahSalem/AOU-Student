import 'package:flutter/material.dart';

const splashBackground = Colors.white;
// Color primaryColor = Color(0xFFA1BE5B);
final ValueNotifier<Color> primaryColor = ValueNotifier<Color>(Colors.teal);
final backgroundColors = Colors.grey[200];